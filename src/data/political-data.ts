// Данные для раздела /political (Политическая реклама).
//
// Сейчас раздел использует тот же набор данных, что и основной сайт.
// Чтобы поменять цены/тексты ТОЛЬКО в политическом разделе, не затрагивая
// основной сайт, замените строку `export * from './data'` ниже на собственную
// копию нужных констант (например, скопируйте PLATFORMS/BLOGGERS/COMMUNITIES
// сюда и отредактируйте значения).
export * from './data';
